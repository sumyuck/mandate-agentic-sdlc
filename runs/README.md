# Curated run evidence

One directory per orchestrated run: the full event log, a timeline, a run summary and a
self-contained HTML report.

Committed on purpose. This is the evidence the system's claims rest on, not build output.
Load it into a local store with `mandate runs import runs`, then verify the audit chains
with `mandate audit verify`.
